/*
    test if dialog receives the correct props
    test if dialog renders the correct content
    test if dialog is overlaying the document
    test dialog's function of 'adding an-already-existing item with same size and same color increases quantity'
*/

import { describe, it } from 'vitest';

describe('CartDialog', () => {
  it.skip('should skip this test', () => {
    // Test code here
  });
});